# Build artifacts
build/
*.obj
*.pdb
*.ilk
*.exe
*.dll
*.tlog
*.log
.worktrees/
*.idb

# ROM files (copyrighted)
*.smc
*.sfc
*.rom

# Extracted ROM data (generated from ROM at build time — copyrighted)
smw_assets.dat
assets/

# Save files
saves/
*.sav
*.srm

# Boot traces (generated at runtime)
boot_trace*.txt

# IDE
.vs/
*.vcxproj.user

# OS
Thumbs.db
.DS_Store

# External repos accessed via junction/symlink (tracked in their own repos)

# Oracle disassembly repo (independent git clone)
SMWDisX/

# Claude auto-memory (per-user)
memory/

# Python caches
__pycache__/
*.pyc

# Local-only config overrides (per-developer, applied after config.ini)
config.local.ini


# Active recompiler output — generated locally per-developer
src/gen/

# Triage scratch
_triage/
hw_trace.log

# Release artifacts (built locally, uploaded via gh release upload)
SuperMarioWorldRecomp-windows-*.zip
SuperMarioWorldRecomp-widescreen-windows-*.zip
release-stage/
release/
