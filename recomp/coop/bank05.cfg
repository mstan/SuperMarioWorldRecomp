# Lunar Magic's $0E:F510 tail trampoline rejoins LoadSublevel here in M1X0.
# Publishing the continuation keeps the level-tilemap initialization out of
# an unbounded interpreter run when the co-op expansion is active.
func LoadSublevelAfterLunarMagic 803F end:809E

# The co-op expansion turns this routine into a mixed-bank dispatch loop
# whose selected targets RTL through the routine's original JSL frame. Keep
# the envelope in the paired interpreter so rewritten returns unwind there
# instead of recursively dispatching the published $05:80C3/$05:80C7
# continuations. Leaf targets can still bounce into their AOT bodies.
hle_func 809E HleCoopInitializeLevelTilemaps

# Expansion-bank JSL helpers use rewritten RTL returns into the middle of
# this routine. Materialize those three architectural continuations so the
# return dispatcher resumes AOT instead of interpreting them as new roots.
func InitializeLevelLayer1And2TilemapsAfterA7D9 80C3
func InitializeLevelLayer1And2TilemapsAfterA84D 80C7
func InitializeLevelLayer1And2TilemapsAfterA5CF 80CB

# The Lunar Magic tail trampoline reached from this entry eventually rejoins
# at $05:80AD after restoring the PHP-saved M/X state.
exit_mx_at 05809e 1 1
