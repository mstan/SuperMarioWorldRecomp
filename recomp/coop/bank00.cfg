# Co-op ExecutePtrLong handlers RTS to this one-byte RTL bridge in the
# FastROM $80 mirror, which then returns long to expansion bank $90.
func LunarMagicReturnLong 84cf end:84d0

# Lunar Magic ExecutePtrLong trampoline used by the co-op hack's game-mode
# wrapper at $90:803D. The wrapper builds a 24-bit handler pointer in DP $00;
# this terminal JMP consumes that pointer. These are the 29 unique targets in
# the 42-entry mode table at $80:9329 (modes $00-$29).
func LunarMagicExecutePtrLong 86df end:86fa
indirect_dispatch 86f7 29 ptrcall return:84cf frame:2 targets:809391,80940F,809F6F,8096AE,809A8B,80941B,809C64,809CD1,809B1A,809DFA,80A087,80A1BE,809F37,80968E,8096D5,80A59C,80A1DA,809750,809759,809468,8094FD,809583,8095AB,8095BC,8095C1,80962C,80963D,809F7C,80968D

# The co-op patch JMLs from this VRAM upload loop into bank $1F, whose
# expanded path JMLs back here. Hardware iterates; direct AOT tail calls would
# recurse in C, so enter the interpreter-tail trampoline at the loop head.
func CoopVramUploadLoop 8726 entry_mx:1,0
hle_func 8726 HleCoopVramUploadLoop
exit_mx_at 008726 1 1

# Co-op/Lunar Magic routes title preparation through long tail trampolines.
# These stock routines are entered M1X1 and restore that width on every RTS;
# publishing the ABI keeps the enclosing one-shot title initializer AOT.
exit_mx_at 9925 1 1
exit_mx_at e2bd 1 1
exit_mx_at c593 1 1

# The generated-tile command dispatcher is entered and returns in native
# 16-bit M0X0 mode; its table-selected bodies make that exit conservative.
exit_mx_at 00bfbc 0 0

# Block-code dispatch is AOT but has multiple table-selected returns; every
# path restores the normal 8-bit accumulator/index ABI before its RTS.
exit_mx_at 00eadb 1 1

# The co-op expansion's $90:BAAA trampoline rejoins the FastROM mirror here.
func RunPlayerBlockCodeAfterCoop eae1 end:eb48
